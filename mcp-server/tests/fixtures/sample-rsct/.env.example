# Example env vars — copy to .env locally
APP_PORT=8080
DATABASE_URL=postgres://localhost:5432/sample
# Secret-shaped key — should be masked
JWT_SECRET=replace-me-with-real-secret-32-chars-min
API_KEY=changeme-very-long-placeholder-value
export NODE_ENV=development
